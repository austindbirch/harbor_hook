apiVersion: v1
kind: Service
metadata:
  name: {{ include "harborhook.fullname" . }}-fake-receiver
  labels:
    {{- include "harborhook.labels" . | nindent 4 }}
    app.kubernetes.io/component: fake-receiver
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.fakeReceiver.service.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harborhook.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: fake-receiver
